import SettingsClient from "@/components/settings/SettingsClient";

export const metadata = {
  title: "Ajustes | CalmBand"
};

export default function SettingsPage() {
  return <SettingsClient />;
}
